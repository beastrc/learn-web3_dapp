{
  "name": "punks",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node http://localhost:8020/ punks",
    "create-local": "graph create --node http://localhost:8020/ punks",
    "remove-local": "graph remove --node http://localhost:8020/ punks",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 punks"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.1",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
