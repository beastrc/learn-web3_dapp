import {Space} from 'antd';
import styled from 'styled-components';

export const StepMenuBar = styled(Space)`
  position: absolute;
  top: 30px;
  right: 30px;
  display: flex;
  flex-direction: row;
`;
